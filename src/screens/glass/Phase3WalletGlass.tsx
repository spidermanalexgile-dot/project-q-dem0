// The banknote treatment was removed in the Wallet redesign — there is now
// only one Wallet (a clean glass numerical display). Re-export so existing
// imports keep working without splitting the codebase.
export { Phase3Wallet as Phase3WalletGlass } from "../Phase3Wallet";
